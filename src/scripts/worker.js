// src/scripts/worker.js
export const workerCode = `
    function adjustPixel(r, g, b, brightnessVal, contrastVal, saturationVal) {
        // ... [Your existing worker code] ...
    }

    self.onmessage = function(e) {
        // ... [Your existing worker message handler] ...
    };
`;